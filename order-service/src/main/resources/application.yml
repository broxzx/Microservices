spring:
  datasource:
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/order
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  application:
    name: order-service
server:
  port: 0